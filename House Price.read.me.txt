House Price Analysis, Feature Engineering, and Price Prediction Using Machine Learning
Developed by DEEPAK.M AI Generalist iPEC Solutions Private Limited Bangalore

Run the cells in order from top to bottom.
Cell 1
import pandas as pd
df = pd.read_csv('data.csv')
Make sure data.csv is in the same folder as the notebook.
Cell 2
df.info()
df.describe()
Verify the dataset loads correctly.
Cell 3
df.isnull().sum()
Check missing values.
Cell 4
Remove unnecessary text columns.
Important Fix
Your notebook creates X before feature engineering.
After:
df["house_age"] = 2026 - df["yr_built"]
df["date"] = pd.to_datetime(df["date"])
df["year"] = df["date"].dt.year
df["month"] = df["date"].dt.month
recreate X:
X = df.drop('price', axis=1)
y = df['price']
Otherwise the new columns are not included.
Encoding
Run:
from sklearn.preprocessing import LabelEncoder
le = LabelEncoder()
for col in ["city", "statezip"]:
    X[col] = le.fit_transform(X[col])
X["date"] = pd.to_datetime(X["date"])
X["date"] = X["date"].astype("int64") // 10**9
Train/Test Split
Train Model
Evaluate
pred = model.predict(X_test)
Then:
from sklearn.metrics import r2_score, mean_squared_error
import numpy as np
print("R2:", r2_score(y_test, pred))
print("RMSE:", np.sqrt(mean_squared_error(y_test, pred)))
Predict New House
Your final section is already correct:
X = df[['sqft_living', 'bathrooms', 'waterfront', 'yr_built']]
y = df['price']
Train:
model.fit(X_train, y_train)
Predict:
new_house = pd.DataFrame({
    'sqft_living': [2500],
    'bathrooms': [3],
    'waterfront': [0],
    'yr_built': [2020]
})
prediction = model.predict(new_house)
print("Predicted Price:", prediction[0])